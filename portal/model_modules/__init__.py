"""Supplemental Django model modules for ArenaLine.

These modules remain part of the ``portal`` Django app; the package only gives
us a cleaner domain-oriented source layout while the legacy ``portal/models.py``
module is gradually decomposed.
"""
